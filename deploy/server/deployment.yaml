apiVersion: apps/v1
kind: Deployment
metadata:
  name: kainotomia-api
spec:
  selector:
    matchLabels:
      app: kainotomia-api
  template:
    metadata:
      labels:
        app: kainotomia-api
    spec:
      containers:
      - name: kainotomia-api
        image: quay.io/hown3d/kainotomia:latest
        resources:
          limits:
            memory: "64Mi"
          requests:
            memory: "32Mi"
            cpu: "10m"
        ports:
        - containerPort: 8080

---

apiVersion: v1
kind: Service
metadata:
  name: kainotomia-callback-service
spec:
  selector:
    app: kainotomia-callback-server
  ports:
  - port: 8080
    targetPort: 8080
